# Netlify configuration for monorepo structure
# The delivery site is in 'frontend'; 'studio' is the authoring tool.

[build]
  base = "frontend"
  command = "npm run build"
  publish = ".next"

# Essential Next.js plugin for Netlify
[[plugins]]
  package = "@netlify/plugin-nextjs"
