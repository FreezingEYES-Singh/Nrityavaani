# Netlify configuration for monorepo structure
# The delivery site is in 'main-website'; 'studio' is the authoring tool.

[build]
  base = "main-website"
  command = "npm run build"
  publish = ".next"

# Essential Next.js plugin for Netlify
[[plugins]]
  package = "@netlify/plugin-nextjs"
